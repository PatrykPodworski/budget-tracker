const {
  COSMOS_DATABASE,
  COSMOS_RAW_CONTAINER,
  COSMOS_ENRICHED_CONTAINER,
  DI_ENDPOINT,
  DI_KEY,
  TEMP_USER_ID,
  OPENAI_ASSISTANT_ID,
  DISCORD_WEBHOOK_URL,
} = process.env;

if (
  !COSMOS_DATABASE ||
  !COSMOS_RAW_CONTAINER ||
  !COSMOS_ENRICHED_CONTAINER ||
  !DI_ENDPOINT ||
  !DI_KEY ||
  !TEMP_USER_ID ||
  !OPENAI_ASSISTANT_ID ||
  !DISCORD_WEBHOOK_URL
) {
  throw new Error("Missing environment variables");
}

export const config = {
  COSMOS_DATABASE,
  COSMOS_RAW_CONTAINER,
  COSMOS_ENRICHED_CONTAINER,
  DI_ENDPOINT,
  DI_KEY,
  TEMP_USER_ID,
  OPENAI_ASSISTANT_ID,
  DISCORD_WEBHOOK_URL,
} as const;
